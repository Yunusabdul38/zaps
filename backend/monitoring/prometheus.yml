global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: blinks-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - blinks-backend:3000
